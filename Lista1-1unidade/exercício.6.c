/*há erros na declaração de main, declaração das variáveis, uso de :, fechamento das aspas do printf, 
posição dos argumentos do printf, uso da variável inexistente d e ausência da biblioteca stdio.h.
*/

#include <stdio.h>

int main()
    {
        int a=1, b=2, c=3;
        printf("os números são: %d %d %d\n", a, b, c);
        return 0;
 }